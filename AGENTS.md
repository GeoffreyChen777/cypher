# 开发工作约定

- 每次完整实现并验证一个功能后，都要重新构建并重启 Dev App，让用户直接使用最新代码，不必等待用户再次要求重启。
- 重启前核对进程身份、工作目录、日志和 IPC 连接；默认只重启开发 UI，保留现有 headless 引擎、聊天和配置。不要影响已安装的正式应用或其他开发实例。若功能确实需要重启引擎，先确认运行中的任务和影响范围。
- 重启后检查新 UI 进程、引擎连接和本次启动日志；最终回复明确说明验证结果。
- Appearance 面板快速迭代阶段：按用户要求逐项改代码、构建并重启 Dev App，不自行渲染截图测试；由用户确认视觉效果。
