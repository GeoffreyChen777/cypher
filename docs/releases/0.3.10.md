# Cypher 0.3.10 — restored main release

Published 2026-09-13 from `cf499ad0e833ae3076a993934b59b1ad4f18cfc2`,
tag `cypher-v0.3.10`, following root commit `27d269b`.
Author and committer: GeoffreyChen777.

Only release metadata changed from restored main: workspace/Edge versions,
landing fallback, and iOS version/build. No sync optimization or v3 runtime
code was introduced.

## Desktop / Linux distribution

- [GitHub release](https://github.com/GeoffreyChen777/cypher/releases/tag/cypher-v0.3.10)
  (release ID 388051343), with 16 assets.
- [CI](https://github.com/GeoffreyChen777/cypher/actions/runs/34785510776)
  and [release workflow](https://github.com/GeoffreyChen777/cypher/actions/runs/34785510640)
  both completed successfully on the tagged source.
- Cloudflare `manifest.json`, `latest.txt`, and the versioned manifest select
  0.3.10. All four public application downloads match their manifest, checksum
  sidecar, and GitHub asset digest/size.
- Downloaded macOS app/DMG passed strict signature, stapled notarization,
  Gatekeeper, disk-image checksum, and executable-version checks.
- Linux x86_64/ARM64 build, CLI, installer, extracted-artifact startup and
  glibc 2.31 checks passed in the release workflow.
- Pi Runtime 0.85.1.2 and its existing immutable objects/channel are unchanged.

The withdrawn v3 release previously used 0.3.10. Users already running that
build or 0.3.11 must **manually install this replacement**. Version comparison
cannot automatically replace an equal version or perform a downgrade. No
automatic conversion of v3 chat databases is claimed.

## Production services

- Edge: `b529c3a0-834f-426a-8922-26626ce16e2d`, 100% traffic.
- Landing: `b5022ae1-5e7c-4525-b8e8-15ce478c4ca4`, 100% traffic, fallback 0.3.10.
- Edge retains migration `v6-remove-sync-v3`; only the five legacy/push DO
  classes are bound, with unchanged namespace IDs. No data deletion occurred
  during this release deployment.
- Public health and `scripts/ci/release.py check-deploy` pass.
- Repository Actions are enabled for CI/release. Automatic `deploy` remains
  disabled; Edge/landing were deployed serially after verified publication.
  The old anomalous queued deployment 34749426934 was not resolved or rerun.

## iOS / development verification

- iOS companion **0.1.6 (5)** uploaded from a manually signed distribution
  archive. Strict signature, production APNs entitlement and privacy checks
  passed; **169 Release simulator tests passed**.
- Apple build `93f7a3f1-4706-4cbe-8cd6-c7d43f98e7ca` is VALID. User-authorized
  `usesNonExemptEncryption=false` was saved for this build. Internal state:
  IN_BETA_TESTING; external state: READY_FOR_BETA_SUBMISSION. No tester
  membership/group changes or review submission were made.
- Additional local sync/doc/proto tests: **172 passed, 2 opt-in tests ignored**.
- Development UI rebuilt as 0.3.10 and launched (PID 93322), connected by
  verified Unix IPC to retained local-only Engine 64075. Unrelated Engine
  37568 was left running; no local data was reset. Startup log contains no errors.
- This is build/distribution and UI/IPC verification, not physical-device
  acceptance or authenticated end-to-end cloud synchronization testing.

Private release evidence is under `/tmp/cypher-restored-0310/` on the release
Mac; deployment and upload logs use `/tmp/cypher-restored-0310-*.log`.
